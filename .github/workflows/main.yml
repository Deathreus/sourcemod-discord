name: CI

on:
  push:
    branches: [ sp-json ]
  pull_request:
    branches: [ sp-json ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sm_version: ['1.9.x', '1.10.x', '1.11.x']

    steps:
      - name: Checkout source
        uses: actions/checkout@v1

      - name: Setup Environment
        run: |
          SOURCEMOD_PATH=$GITHUB_WORKSPACE/addons/sourcemod
          echo ::set-env name=SOURCEMOD_PATH::$SOURCEMOD_PATH
          echo ::set-env name=SCRIPTS_PATH::$SOURCEMOD_PATH/scripting
      
      - name: Setup SourcePawn Compiler
        uses: rumblefrog/setup-sp@master
        with:
          version: ${{ matrix.sm_version }}

      - name: Create directory
        run: mkdir -p compiled
        
      - name: Compile Plugins
        run: |
          spcomp -i $SCRIPTS_PATH/include $SCRIPTS_PATH/discord_api.sp
          spcomp -i $SCRIPTS_PATH/include $SCRIPTS_PATH/discord_test.sp

      - name: Upload plugin artifact
        uses: actions/upload-artifact@master
        with:
          name: Plugin
          path: compiled/
